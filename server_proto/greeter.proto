syntax = "proto3";
package proto;
service Greeter {
    rpc Hello(HelloRequest) returns(HelloRespone) {}
}


message HelloRequest{
    string name = 1;
}

message HelloRespone{
    uint32 pid = 2;
}